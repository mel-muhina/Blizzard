describe("questions controller", () => {
  describe("show", () => {
    beforeEach(() => {
      jest.clearAllMocks();
    });

    it("Responds with 200 code and a question", async () => {});
  });
});
